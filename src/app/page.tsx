"use client";
import React, { FC } from "react";
import { MainPageContent } from "@/widgets/main-page-content";

const Home: FC = () => {
  return (
    <main>
      <MainPageContent />
    </main>
  );
};

export default Home;