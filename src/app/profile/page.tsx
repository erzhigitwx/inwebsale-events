"use client";

import React, { FC } from "react";
import { ProfileContent } from "@/widgets/profile-content";

const Profile: FC = () => {
  return (
    <ProfileContent/>
  );
};
export default Profile;